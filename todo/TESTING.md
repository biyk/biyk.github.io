# План тестирования todo-проекта

Проект работает и выполняет необходимые задачи. План составлен, чтобы не потерять функционал, не затереть и не сломать данные при внесении правок.

## Принципы

- Одна группа правок → прогон регресса → следующий шаг.
- Никаких записей в боевую Google-таблицу из тестов: тесты работают только с временной копией.
- После правок — прогон браузерных тестов и ручной регресс.

---

# Реализованные тесты

## Браузерные (кнопка «Тесты»)

Запуск: кнопка «Тесты» на вкладке «Настройки» (раннер `src/utils/tests/index.js`, автоподхват `*.test.js`). Порядок прогона = алфавитный порядок имён файлов → имена имеют числовые префиксы.

Поток прогона:
1. Создаётся копия боевой таблицы (`gapi.client.drive.files.copy`, ID из настройки `spreadsheetId`) → общий `testId`.
2. Все кейсы гоняются против `testId` (боевая таблица не читается и не пишется).
3. В `finally` копия удаляется (и при ошибке); если удаление не сработало — в консоль логируется ID для ручной уборки.
4. Результаты стримятся в UI по мере выполнения («Выполняется…»), дубль в консоль.

Контекст теста (`run(ctx)`): `ctx.$store`, `ctx.$el` (Vue-прокси, доступны через прототип), `ctx.testId` (ID общей копии), `ctx.setTestId(id)` (устанавливается тестом создания копии).

| № | Файл | Проверка |
|---|---|---|
| 01 | `01-button-visible.test.js` | Кнопка «Тесты» отображается |
| 03 | `03-copy-creation.test.js` | Создание копии таблицы, `ctx.setTestId` |
| 04.01 | `04-headers-sheet.test.js` | Шапки листов копии совпадают с ожидаемыми |
| 04.02 | `04.02-row-counters.test.js` | В первой колонке листов нет дублей |
| 04.03 | `04.03-addrow.test.js` | `addRow` не плодит дубли |
| 04.04 | `04.04-addrows.test.js` | `addRows` не плодит дубли |
| 04.05 | `04.05-update-row-by-code.test.js` | `updateRowByCode` обновляет строку, а не добавляет новую |
| 04.06 | `04.06-delete-row.test.js` | `deleteRow` удаляет нужную строку |
| 04.07 | `04.07-write-serialization.test.js` | Параллельные записи идут FIFO, не теряются |
| 04.08 | `04.08-custom-id-columns.test.js` | Запись с кастомным `spreadsheetId` кладёт данные по своим колонкам |
| 04.09 | `04.09-calendar-list.test.js` | События на сегодня загружаются |
| 04.11 | `04.11-calendar-tz.test.js` | Таймзона слотов корректна |
| 06 | `06-cleanup-copy.test.js` | Копия удаляется (404 при удалении несуществующего = OK) |

Примечание: тест 04.10 (дубли по `task_uuid`) удалён — он ничего не диагностировал (дубли в календаре бывают нормально, по решению пользователя проверку убрали).

## Node-тесты (vitest)

Запуск из консоли/CI: `npm test` (в `todo/src`). Цель — зафиксировать текущий функционал и ловить деградацию кода без браузера (быстро, до браузерного прогона).

- Раннер: `vitest run`, конфиг `src/vitest.config.js` — include только `utils/tests/unit/**`, браузерный раннер (glob `./*.test.js`) эти файлы не подхватывает.
- Среда: чистый Node, без jsdom. Браузерные глобалы (`gapi`, `window`, `localStorage`, `sessionStorage`) — стабы в `unit/_globals.js`.
- Структура: `src/utils/tests/unit/*.test.js` — чистая логика без сети (ORM, formatData, getFreeSlots, taskSort/taskDate).
- Известные баги (todo.md, «Устойчивость к сбоям») оформлены как `it.todo(...)` со ссылкой на пункт — прогон остаётся зелёным; при починке `todo` превращается в полноценный тест (см. список в конце документа).

| Файл | Покрытие |
|---|---|
| `unit/orm.test.js` | `ORM.getRaw`/`getFormated`: round-trip, строка короче колонок, чанки >49000 симв. |
| `unit/formatData.test.js` | `Table.formatData`: простые значения, JSON-чанки |
| `unit/freeSlots.test.js` | `getFreeSlots`: слоты, события встык, min-слот 15 мин |
| `unit/taskSort.test.js` | `taskSort`, `taskDate` |

---

# Инфраструктура тестов

## Файлы

| Файл | Назначение |
|---|---|
| `src/utils/tests/index.js` | Раннер кнопки: `Object.create(ctx)` (сохраняет Vue-прокси `$store`/`$el` через прототип), `testId`/`setTestId`, `onResult`, `ensureGapi` (sheets+drive+calendar) |
| `src/utils/tests/*.test.js` | Браузерные кейсы; порядок = алфавит имён (числовые префиксы `01-`, `03-`, `04-`, `04.xx-`, `06-`) |
| `src/utils/tests/unit/*.test.js` | Node-кейсы (vitest), браузерный раннер их не подхватывает |
| `src/components/Settings.vue` | Кнопка «Тесты» + панель результатов (`runTests(this, r => this.testResults.push(r))`, «Выполняется…» пока пусто) |

## Ключевые классы/функции под тестами

- `Table` / `ORM` / `GoogleSheetDB` из `dnd/static/js/db/google.js` — общий слой данных (todo и dnd). После правок здесь — обязательно прогон регресса.
- `src/utils/tasks.js` — `makeTaskDone`, `setTaskToCalendar`, `setTaskCompleted`, `calcExecutions`, `taskSort`.
- `src/utils/calendar.js` — `listEvents`, `addEvent`, `updateEvent`, `deleteEvent`, `getFreeSlots`, `makeEvent`.
- `src/utils/uuid.js` — `generateUUIDv4()` (для уникальных имён задач в сценариях).

## Политика безопасности данных

- Read-only тесты — только против копии таблицы, боевая таблица не читается и не пишется тестами.
- Запись-тесты (add/update/delete) — только против копии, после прогона копия удаляется.
- Calendar: существующие тесты — только read-only. Планируемые UI-сценарии (пишут в календарь через `toggleTodo`/`deleteTodo`) будут гоняться в тест-режиме (`setCalendarTestMode`) — без реальных событий в календаре.

---

# Ручной регресс A–K (после правок)

Прогон на живом приложении (dev + prod) до и после каждой группы правок.

### A. Сборка и запуск
- [ ] `npm run build` без ошибок; `todo/index.html` ссылается на свежие хешированные assets
- [ ] Приложение грузится в dev и с прода
- [ ] Нет ошибок в консоли при загрузке
- [ ] Бандл ~1 МБ (minify включён), sourcemap присутствует

### B. Авторизация
- [ ] Authorize → попап Google → токен в `localStorage`
- [ ] Перезагрузка страницы сохраняет авторизацию
- [ ] Sign Out удаляет токен
- [ ] Истёкший токен → запрос re-auth

### C. Задачи: просмотр и фильтры
- [ ] Вкладки «Сейчас/Сегодня/Завтра/Все» дают корректный набор
- [ ] Счётчик задач и суммарные часы корректны
- [ ] Сортировка не изменилась
- [ ] Выполненные задачи (`colorId=7`) скрываются
- [ ] Список обновляется реактивно

### D. Задачи: действия
- [ ] Добавление задачи сразу видно в списке
- [ ] Старт → таймер идёт, `start_date` установлен
- [ ] Пауза → фактическое поведение без изменений (сейчас = завершение + сброс таймеров)
- [ ] Завершение → деньги начислены + событие `colorId=7` в календаре + запись в `task_executions`
- [ ] Завершение с таймером → `minutesSpent` = потраченное время, `task_time` усреднён
- [ ] Удаление → событие удалено, денег НЕТ, `task_date` сдвинут
- [ ] Правка описания сохраняется

### E. Календарь
- [ ] «Заполнить календарь» создаёт события в свободных слотах (проверка в Google Calendar)
- [ ] Повторный запуск без дублей событий
- [ ] `excludes` уважаются

### F. Герой и деньги
- [ ] `hero_money` меняется ровно на `money_reward = minutesSpent * averageCalc / 2`
- [ ] При первом открытии нет NaN
- [ ] Запись в `real_life_hero` обновляет строку, а не добавляет

### G. Магазин
- [ ] Список товаров грузится
- [ ] Покупка: баланс −cost, `rewards_history` +1, `reward_done` +1, `reward_cost` += step
- [ ] Обновляются существующие строки, а не дубли
- [ ] Покупка при балансе < стоимости блокируется, минус невозможен

### H. Статистика
- [ ] today/week/month/h24/d7 правдоподобны
- [ ] averageCalc/prevAvg/today_points отображаются
- [ ] Нет падения на строках с пустыми ячейками

### I. Настройки
- [ ] Добавление/удаление настроек сохраняется и переживает перезагрузку
- [ ] Без `spreadsheetId` приложение не падает
- [ ] Кнопка «Тесты» есть и прогоняет набор

### J. Фоновый агент
- [ ] Задачи/события синхронизируются каждые 60 сек без действий пользователя
- [ ] Остановка агента без утечек интервала

### K. Целостность данных в Sheets (главный риск «затереть»)
- [ ] После каждого действия счётчик строк в листах не растёт лишними дублями
- [ ] Шапки листов не тронуты
- [ ] `task_executions` только добавляется, старые строки не меняются

---

# Проверка после деплоя

- [ ] Продакшн-версия: загрузка, авторизация
- [ ] Одно завершение задачи до/после
- [ ] Одна покупка в магазине
- [ ] Все вкладки и старые функции на месте
- [ ] Кнопка тестов работает на проде

---

# Нереализованные тесты

Ниже — все тесты, которые ещё не написаны, в порядке приоритета реализации. В скобках — ID для привязки к планам в todo.md (Уст.…, O.…). Отмеченные «it.todo» уже оформлены как заглушки в unit-файлах: прогон зелёный, при починке бага заглушка превращается в полноценный тест.

## Что нужно из кода для этих тестов

- `setCalendarTestMode(true)` в `src/utils/calendar.js` — `addEvent/updateEvent/deleteEvent` → no-op с записью вызовов в память, `listEvents` возвращает управляемый список. Без этого `toggleTodo`/`deleteTodo` создают реальные события в календаре пользователя (нужно для всех 05.x).
- `src/utils/tests/helpers.js` (не существует) — `waitFor` (polling с учётом `setTimeout` 300/600 мс и throttle 1000 мс), DOM-клики, swap/restore настроек (`NEW_SETTINGS` + `todos/initTodos` + `hero/initHero` + `listEvents`), читалки листов копии, `withGapiStub(apiPath, stubFn, asyncFn)` — подмена и восстановление gapi-метода.
- Правки из todo.md «Устойчивость к сбоям» (04.12–04.19, 07) и «Оффлайн-очередь действий» (08.x) — без них соответствующие тесты остаются красными (в этом их смысл).
- `Outbox` в `dnd/static/js/db/` с чистыми `enqueue`/`flush`/`isRetryableError` (08.x) и тест-хелпер `clearOutbox()` — очистка очереди в `finally`.
- Для UI-сценариев нужны уникальные имена задач (таймстамп-суффикс) и проверки по конкретным строкам, а не по глобальным счётчикам (копия к моменту прогона уже содержит данные от 04.x).

## Нумерованный список

1. **05.01 — Добавить задачу** — строка появилась в DOM и в `real_life_tasks` с дефолтами (task_time 15, money_reward 7, repeat_mode 6, start_date 0).
2. **05.02 — Старт** — `start_date` записан, в DOM виден таймер.
3. **05.03 — Старт → Пауза** — задача завершена: запись в `task_executions`, `hero_money` += награда, `number_of_executions` +1, `task_finish_date`=1, `start_date`=0. Фиксирует фактическое поведение (сейчас ⏸ = `toggleTodo` + сброс таймеров, TodoList.vue:315-326).
4. **05.04 — Выполнить без таймера** — награда, сдвиг `task_date` по `repeat_mode`, запись исполнения.
5. **05.05 — Старт → Выполнить с таймером** — `minutesSpent` = потраченное время, `task_time` усреднён.
6. **05.06 — Удалить** — задача перенесена на завтра, `repeat_index` +0.1, событие календаря удалено.
7. **05.07 — Комбо** — несколько задач (старт/пауза/выполнение/удаление) → сходятся суммы: `hero_money` = Σ наград − покупки, `task_executions` = числу выполнений, нет дублей по `task_uuid`.
8. **05.08 — Магазин** — покупка: `hero_money` − цена, запись в `rewards_history`, `reward_done` +1, `reward_cost` += шаг.
9. **04.12 — `formatData` на битых данных** — не падает на пустых/коротких строках и битом JSON, битые строки пропускаются. Сейчас ❌ (TypeError/throw).
10. **04.13 — `getFormated` на короткой строке** — нет throw, недостающие ключи `undefined`. Сейчас ⚠️ падает при `columns=undefined`.
11. **04.14 — `getFreeSlots` с невалидными событиями** — события без дат, `NaN`-даты, all-day (`date` без `dateTime`) → слоты корректны, нет NaN. Сейчас ⚠️ молча ломает слоты.
12. **04.15 — `fetchSheetValues` при сетевой ошибке** — возвращает признак ошибки, а не `[]`; кэш, `columns` и `codes` не перетираются. Сейчас ❌ возвращает `[]`, ломает кэш.
13. **04.16 — `updateRowByCode` при сбое refresh** — бросает, но НЕ добавляет дубль-строку. Сейчас ❌ плодит дубль через `addRow`.
14. **04.17 — `todos/initTodos` при ошибке чтения** — не очищает `localStorage['todo-list']`, не затирает state. Сейчас ❌ стирает localStorage.
15. **04.18 — `listEvents` при ошибке сети** — не бросает (fallback/старые события), агент и `mounted` не падают. Сейчас ❌ бросает.
16. **04.19 — Битый localStorage** — `JSON.parse` в `loadTodos`/`loadFromStorage` → фолбэк на пустой массив, приложение не падает. Сейчас ❌ краш при старте.
17. **05.09 — «Сеть упала во время выполнить»** — мок `addEvent`/записи на ошибку → задача НЕ помечается выполненной (или откат), деньги не начислены, `task_executions` пусто; повтор после восстановления сети не задваивает награду.
18. **05.10 — «Сеть упала во время покупки»** — сбой между шагами `buyProduct` → баланс не списан без записи в `rewards_history`/обновления товара (откат); повторная покупка корректна, стейл-кэш баланса не даёт двойного списания.
19. **05.11 — «Сеть упала во время паузы/выполнения с таймером»** — `minutesSpent` не потерян, состояние консистентно (localStorage = таблица), повторная попытка не ломает `repeat_index`/награду.
20. **07 — `taskAgent` переживает ошибку сети** — `initTodos`/`listEvents` при провале не дают необработанных rejection'ов, интервал продолжает работать. Сейчас ❌ rejection каждые 60 с.
21. **08.01 — outbox: персистентность** — `enqueue` → запись в localStorage, очередь переживает «перезагрузку» (re-init модуля).
22. **08.02 — outbox: FIFO** — порядок реплея = порядок записи (mock gapi считает вызовы).
23. **08.03 — outbox: retryable** — `isRetryableError`: сеть/429/401/5xx → остаётся в очереди; 404/схема → `failed` и удаляется.
24. **08.04 — outbox: flush** — после «восстановления сети» очередь очищается, листы получают все действия.
25. **08.05 — outbox: идемпотентность** — «запись прошла, ответ потерян» → реплей не плодит дубли (`execution_id`/`item_id`/`task_uuid`).
26. **08.06 — outbox: частичный сбой покупки** — обрыв `buyProduct` между шагами → в очереди только невыполненные шаги, реплей доводит до консистентного состояния.
27. **08.07 — outbox: backoff** — 429 → backoff, очередь не дёргается без конца, попытки лимитированы.
28. **08.08 — outbox: снапшот** — реплей пишет снапшот (`execution_date`/`execution_time` из действия), а НЕ пересчитывает на момент реплея.
29. **05.12 — «Выполнено оффлайн → авто-flush»** — награда локально + бейдж «N действий ожидают»; после «возврата сети» (mock) → flush → `task_executions` и `hero_money` сходятся, нет дублей.
30. **05.13 — «Покупка оффлайн»** — списание + история в очереди, реплей консистентен, нет двойного списания (баланс из локального `hero`).
31. **05.14 — «Пауза/выполнение с таймером оффлайн»** — `minutesSpent` из кэша действий, реплей без двойной награды, state = таблица после flush.
32. **formatData на строке без значения (Уст.3)** — `it.todo` в `unit/formatData.test.js`.
33. **formatData на битом JSON в value (Уст.3)** — `it.todo` в `unit/formatData.test.js`.
34. **`getFormated` при `columns=undefined` (Уст.2)** — `it.todo` в `unit/orm.test.js`.
35. **`getFreeSlots` с невалидными датами (Уст.12)** — `it.todo` в `unit/freeSlots.test.js`.
36. **`getFreeSlots` с all-day событиями в локальной таймзоне (Уст.12)** — `it.todo` в `unit/freeSlots.test.js`.
